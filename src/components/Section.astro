---
interface Props {
  disableAnimation?: boolean;
  id?: string;
}

const { disableAnimation = false, id } = Astro.props;
---

<section
  id={id}
  class={`flex-col scroll-mt-32 my-[16px] ${!disableAnimation && "fade-in"}`}
>
  <slot />
</section>
